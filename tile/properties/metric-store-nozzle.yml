---
- name: metrics_provider_mtls
  label: Metrics Provider (RLP) mTLS Certificate
  type: rsa_cert_credentials
  configurable: false
  default:
    domains:
    - metric-store
    extended_key_usage:
    - client_auth
    - server_auth
- name: disable_platform_and_system_metrics
  type: boolean
  default: false
  configurable: true

