---
- name: metric_store_mtls
  label: Metrics Store Egress mTLS Certificate
  type: rsa_cert_credentials
  configurable: false
  default:
    domains:
    - metric-store
    - metric_store
    - localhost
    extended_key_usage:
    - client_auth
    - server_auth
