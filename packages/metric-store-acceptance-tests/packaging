set -ex

export GOPATH=$BOSH_INSTALL_TARGET
export GOROOT=$(readlink -nf /var/vcap/packages/golang1.12.1)
export PATH=$GOROOT/bin:$PATH

mkdir -p cache
export GOCACHE=$PWD/cache

# TODO: this doesn't exist yet
# go build -mod=vendor -o ${BOSH_INSTALL_TARGET}/metric-store-acceptance-tests ./src/cmd/acceptance-tests
